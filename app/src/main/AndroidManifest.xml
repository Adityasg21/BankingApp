<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bankingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sendtouser"
            android:screenOrientation="portrait"
            android:label="Transfer To"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".history_list"
            android:parentActivityName=".users_list"
            android:screenOrientation="portrait"
            android:label="Transactions"
            android:theme="@style/AppTheme"/>
        <activity android:name=".user_data"
            android:parentActivityName=".users_list"
            android:screenOrientation="portrait"
            android:label="Account:"
            android:theme="@style/AppTheme"/>
        <activity android:name=".users_list"
            android:screenOrientation="portrait"
            android:label="Users IN Bank"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".splash_screen_activity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>